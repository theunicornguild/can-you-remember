### Git

Create a new checkpoint

```shell
$ git add .
$ git commit -m "Done with player mode handling"
$ git push
```

---

### Trello

This means you finished the card

> "As a player I can choose between single and multiplayer mode"

Move the card from the `Doing` list to `Done`
